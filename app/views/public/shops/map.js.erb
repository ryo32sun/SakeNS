
let map;
function initMap(){
  geocoder = new google.maps.Geocoder()
  map = new google.maps.Map(document.getElementById('map'), {
  center: {
    <!--lat: gon.shop.latitube,-->
    <!--lng: gon.shop.longitube-->
    lat: <%= @shop.latitube %>,
    lng: <%= @shop.longitube %>
  },
  zoom: 12,
  });
  
  merker = new google.maps.Marker({
    position: {
      <!--lat: gon.shop.latitube,-->
      <!--lng: gon.shop.longitube-->
      lat: <%= @shop.latitube %>,
      lng: <%= @shop.longitube %>
    },
    map: map
  })
}
